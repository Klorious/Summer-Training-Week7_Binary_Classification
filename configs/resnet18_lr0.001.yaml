model_name: "resnet18"
pretrained: True
learning_rate: 0.001
batch_size: 32
epochs: 30
optimizer: "AdamW"
input_size: 224
weight_decay: 0.0001
random_seed: 20260725
freeze_backbone: false
augmentation: "Resize(256), RandomResizedCrop(224), RandomHorizontalFlip, ImageNet normalization"
